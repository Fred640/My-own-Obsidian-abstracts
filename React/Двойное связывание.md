`function App() {`
  `const [text, chtext] = useState("dokfokisdf")`
    `return (`
    `<div className='App'>`
      `<h2>{text}</h2>`
      `<input` 
      `value={text}`
      `onChange={event => chtext(event.target.value)}`
      `>`
      `</input>`
    `</div>`
  `);`
`}`

[[Состояния]]