useMemo(callback, deps) - первым параметром принимает callback  а вторым параметром массив зависимостей

callback должен возращать результат каких то вычислений 

useMemo производит вычисления, запоминает результат этих вычислений, и кэширует. Подобное повидение называеться мемоизация